import { IGX_PREFIX, prefixResource } from 'igniteui-i18n-core';
import { ComboResourceStringsDA as AComboResourceStrings } from 'igniteui-i18n-resources';

/**
 * Danish resource strings for IgxCombo
 */
export const ComboResourceStringsDA = prefixResource(IGX_PREFIX, AComboResourceStrings);
